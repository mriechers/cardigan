# Cardigan Web Dashboard

# API key for WebSocket authentication.
# Must match the server's CARDIGAN_API_KEY.
# When empty or absent, connects without auth (dev mode).
# In Docker/nginx deployments, REST calls get the key injected by nginx,
# but WebSocket connections still need this for the token query param.
VITE_CARDIGAN_API_KEY=
